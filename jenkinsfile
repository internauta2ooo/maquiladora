pipeline {
  agent any
  tools {
    nodejs 'testme'
  }

  options {
    timeout(time: 2, unit: 'MINUTES')
  }

  stages {
    stage('Install dependencies') {
      steps {
        bat 'cd jenkins-tests && npm i'
      }
    }
    stage('Run tests') {
      steps {
        bat 'npm t'
      }
    }
  }
}
